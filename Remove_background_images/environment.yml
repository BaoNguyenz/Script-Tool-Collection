name: rembg_tool
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - pillow
  - tqdm
  - cuda-toolkit=12.1  # CUDA libraries for GPU acceleration
  - cudnn  # cuDNN from conda-forge (auto-detects compatible version)
  - pip:
    - rembg[gpu]
    - onnxruntime-gpu
